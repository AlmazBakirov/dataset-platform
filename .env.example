# Dataset Platform UI config

# 1 = use mock backend, 0 = real backend
USE_MOCK=1

# Backend base URL
BACKEND_URL=http://localhost:8000

# Request timeout (seconds)
REQUEST_TIMEOUT_S=20

# Upload mode: mvp | presigned
UPLOAD_MODE=mvp
